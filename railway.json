{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "gunicorn --bind 0.0.0.0:$PORT --chdir Backend/Python app:app",
    "restartPolicy": "on-failure"
  }
}